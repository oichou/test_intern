# test_intern

Pour lancer les tests, il est nécessaire au préalable d'avoir Java et Chrome d'installés dans son environnement. Pour WSL/Ubuntu :

```sh
sudo apt update && sudo apt upgrade
sudo apt install default-jre firefox
```
